[package]
name                   = "sideromelane-core"
version                = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
description            = "Core domain crate for the Sideromelane macOS desktop app."
readme                 = "../../README.md"
keywords               = ["desktop", "local", "macos"]
categories             = ["gui"]

[lib]
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
ignore     = "0.4"
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
